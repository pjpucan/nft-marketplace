<?php
if (!defined('ABSPATH')) {
    exit; // Exit if accessed directly.
}

$block_name = basename(dirname(__FILE__)); // Block folder name as block name
$block_title = 'Collections';
$block_description = '';
$block_icon = 'category';
$block_keywords = array( 'collections' );