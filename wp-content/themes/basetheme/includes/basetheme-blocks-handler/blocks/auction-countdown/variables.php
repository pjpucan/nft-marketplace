<?php
if (!defined('ABSPATH')) {
    exit; // Exit if accessed directly.
}

$block_name = basename(dirname(__FILE__)); // Block folder name as block name
$block_title = 'Auction Countdown';
$block_description = '';
$block_icon = 'clock';
$block_keywords = array( 'auction countdown' );